# Charts Components

This directory is dedicated to Recharts-based data visualizations.
- Put your chart sub-components here (e.g. `health-trends.tsx`, `spray-savings.tsx`).
- Keep chart styling responsive and dark-mode compatible by using Tailwind variables.
