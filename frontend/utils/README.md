# Utilities Directory

This directory is for client-side formatting, parsing, mathematical calculations, and pure helper functions.
- For UI class utilities and shadcn merging, use `@/lib/utils.ts`.
- Custom hooks can be placed in a separate `@/hooks` folder.
