# Components Directory

This directory contains reusable UI components for the EcoGuard AI Next.js frontend.
- `ui/`: Core shadcn/ui library components (buttons, input fields, tables, alerts).
- `charts/`: Specialized visualization wrappers (Recharts) for NDVI and pesticide metrics.
- `theme-provider.tsx`: Controls dark/light system-wide color modes.
