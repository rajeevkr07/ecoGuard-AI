# Model Context Protocol (MCP) Server

This directory contains the MCP server and custom tools exposed to Gemini/ADK.
- `tools/`: Python scripts defining specific tools (soil telemetry, drone actions, etc.)
- `server.py`: Server runner initializing the MCP.
